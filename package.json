{
  "name": "<%= appName %>",
  "version": "<%= version %>",
  "author": "<%= realname %>",
  "description": "<%= appDescription %>",

  "buildDir": "../../public",

  "devDependencies": {

  },

  "peerDepenencies": {
    "grunt-cli": "*",
    "bower": "*"
  },

  "scripts": {
    "postinstall": "bower install -s"
  }
}